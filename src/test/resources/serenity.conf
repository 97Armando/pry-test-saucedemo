webdriver {
  driver = edge
  capabilities {
    "edge:edgeOptions" {
      args = ["--start-maximized", "--remote-allow-origins=*"]
    }
  }
}

environments {
  default {
    webdriver.base.url = "https://www.saucedemo.com/"
  }
}